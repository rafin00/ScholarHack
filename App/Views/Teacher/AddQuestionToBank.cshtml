@model Repo.QuestionBank
@{
    ViewBag.Title = "AddQuestionToBank";
}

<h2>AddQuestionToBank</h2>
<form method="post">
@Html.ActionLink("Import from File","ImportFile", new {QuestionBankID=Model.QuestionBankID });
    
    <div id="manual">
      

    Question : <input name="QuestionText" /><br />
    Difficulty : 
        @foreach (var difftype in TempData["QuestionDifficulty"] as IEnumerable<Repo.QuestionDifficulty>)
            {

            <input type="radio" name="QuestionDifficultyID" value="@difftype.QuestionDifficultyID" />@difftype.QuestionDifficultyName

        }
    Answer : <input name="QuestionAnswer" /> <br />
       

        Question : <input name="QuestionText" /><br />
        Difficulty :
        @foreach (var difftype in TempData["QuestionDifficulty"] as IEnumerable<Repo.QuestionDifficulty>)
            {

            <input type="radio" name="QuestionDifficultyID" value="@difftype.QuestionDifficultyID" />@difftype.QuestionDifficultyName

        }
        Answer : <input name="QuestionAnswer" /> <br />
       

        <input type="submit" value="Add"><br />
        <</div>

       </form>